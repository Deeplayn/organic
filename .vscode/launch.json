{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug ORGANIC Locally",
            "type": "pwa-chrome",
            "request": "launch",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "Start OrganoChem Dev Server"
        }
    ]
}
